Person:
  type: object
  properties:
    id: { type: integer }
    name: { type: string }
    email: { type: string, format: email }
PersonCreate:
  type: object
  properties:
    name: { type: string }
    email: { type: string, format: email }
